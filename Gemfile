source "https://rubygems.org"

# GitHub Pages compatible build. Used for the GitHub Actions deploy and
# for previewing locally with `bundle exec jekyll serve`.
gem "jekyll", "~> 4.3"

# Faster local rebuilds on macOS/Linux.
gem "webrick", "~> 1.8"
